# OpenAI API key — https://platform.openai.com/api-keys
OPENAI_API_KEY=sk-...

# JWT secret — generate a strong one with:
#   python -c "import secrets; print(secrets.token_hex(32))"
JWT_SECRET=change-this-to-a-long-random-string
